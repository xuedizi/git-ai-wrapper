# v1.7.0-tac.v0.3.1

Add WorkBuddy support in the existing `codebuddy-preset.patch`.

- Share CodeBuddy hook parsing, model extraction, and cwd fallback.
- Preserve separate `workbuddy` attribution and session identities.
- Register WorkBuddy hook installation at `~/.workbuddy/settings.json`, including `install-hooks --target workbuddy`.
- Cover committed attribution for file and Bash edits, isolated hook installation, idempotency, and preservation of CodeBuddy configuration.

Validation: 2,437 library tests passed (1 ignored), 13 CodeBuddy/WorkBuddy integration tests and 3 targeted-install integration tests passed locally. All four downstream patches apply to the pinned upstream v1.7.0.
